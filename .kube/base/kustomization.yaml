apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: fiftymoves

resources:
  - config.yaml
  - data.yaml
  - api.yaml
  - web.yaml
